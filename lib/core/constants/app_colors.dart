import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(
    0xFF01B0A1,
  ); // Teal / Green based on the logo
  static const Color secondary = Color(0xFF00796B);
  static const Color background = Color(0xFFF5F5F5);
  static const Color white = Colors.white;
  static const Color black = Color(0xFF121212);
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);

  // Tracker Condition Colors
  static const Color conditionGood = Color(0xFF4CAF50);
  static const Color conditionWarning = Color(0xFFFFC107);
  static const Color conditionBad = Color(0xFFF44336);
}
